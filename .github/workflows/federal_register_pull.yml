name: Monthly Intelligence Pull (Multi-Tier)

on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  extract-and-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 google-genai

      - name: Run Tier 1 Pull
        run: python pull_data.py
        
      - name: Run Tier 2 Pull
        env:
          SB_API_KEY: ${{ secrets.SB_API_KEY }}
          SB_SHARED_KEY: ${{ secrets.SB_SHARED_KEY }}
        run: python tier2_pull.py
        
      - name: Run Tier 3 Enrichment
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python tier3_enrich.py

      - name: Commit and Push Artifacts
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add latest_pull.json latest_tier2_pull.json master_data.json
          git commit -m "Automated CTI pull: Multi-Tier Extraction & Enrichment" || echo "No changes to commit"
          git push